# Install on the real-robot computer only after confirming its Python and
# controller/firmware compatibility.  The trajectory preview uses only the
# Python standard library and does not require this package.
xarm-python-sdk>=1.16,<2
numpy>=1.24,<3
scipy>=1.10,<2
